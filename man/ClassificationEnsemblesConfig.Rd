% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClassificationEnsembles.R
\name{ClassificationEnsemblesConfig}
\alias{ClassificationEnsemblesConfig}
\title{Configuration Parameters Matrix for Classification Pipeline}
\usage{
ClassificationEnsemblesConfig(
  cv_folds = 5,
  train_pct = 0.75,
  vif_threshold = 5,
  transform_steps = c("nzv", "medianImpute", "center", "scale"),
  rf_tune = 5,
  glmnet_grid = expand.grid(alpha = seq(0, 1, length = 4), lambda = seq(0.001, 0.1,
    length = 5)),
  svm_tune_length = 5,
  nnet_tune_length = 5,
  tree_tune_length = 5,
  leverage_threshold = 999
)
}
\arguments{
\item{cv_folds}{Integer. The number of cross-validation folds. Default is 5.}

\item{train_pct}{Numeric. The training data proportion as a decimal between 0 and 1. Default is 0.75.}

\item{vif_threshold}{Numeric. Variance Inflation Factor threshold for filtering multicollinear predictors. Default is 5.}

\item{transform_steps}{Character vector. Preprocessing transforms to pass to caret. Default is c("nzv", "medianImpute", "center", "scale").}

\item{rf_tune}{Integer. Tuning length parameter for Random Forest/Ranger models. Default is 5.}

\item{glmnet_grid}{Optional custom tuning grid for Elastic Net models.}

\item{svm_tune_length}{Integer. Tuning length parameter for SVM models. Default is 5.}

\item{nnet_tune_length}{Integer. Tuning length parameter for Neural Network models. Default is 5.}

\item{tree_tune_length}{Integer. Tuning length parameter for tree-based models (Rpart, C5.0, gbm). Default is 5.}

\item{leverage_threshold}{Numeric. Outlier removal multiplier relative to mean hatvalues. 999 deactivates filter. Default is 999.}
}
\value{
A structured list containing isolated operational tuning parameters.
}
\description{
Configuration Parameters Matrix for Classification Pipeline
}
